# how to install docker on  amazon linux easily 
# Amazon Linux
  #you can use userdata to do the smae thing too
###########################
sudo yum update -y    
sudo yum install docker -y
sudo service docker start
sudo usermod -aG docker ec2-user
sudo su - ec2-user
